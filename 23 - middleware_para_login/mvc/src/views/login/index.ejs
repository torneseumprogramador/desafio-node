<main class="container mx-auto p-8 text-center">
    <h1 class="text-4xl font-bold text-blue-600">Login</h1>

    <form action="/login" method="POST" class="mt-6 bg-white shadow-md rounded-lg p-6 max-w-md mx-auto text-left">
        <!-- Usuário -->
        <div class="mb-4">
            <label for="email" class="block text-gray-700 font-bold mb-2">Email</label>
            <input type="text" id="email" name="email" required
                class="w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 outline-none">
        </div>

        <!-- Senha -->
        <div class="mb-4">
            <label for="senha" class="block text-gray-700 font-bold mb-2">Senha</label>
            <input type="password" id="senha" name="senha" required
                class="w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 outline-none">
        </div>

        <!-- Me deixe logado -->
        <div class="mb-4 flex items-center">
            <input type="checkbox" id="lembrar" name="lembrar"
                class="mr-2 w-5 h-5 text-blue-600 border-gray-300 rounded">
            <label for="lembrar" class="text-gray-700">Me deixe logado</label>
        </div>
        
        <% if (error_msg.length > 0) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <span class="block sm:inline"><%= error_msg %></span>
            </div>
            <br>
        <% } %>

        <!-- Botão de Login -->
        <button type="submit"
            class="w-full bg-blue-500 text-white px-6 py-3 rounded-md hover:bg-blue-600 transition">
            Entrar
        </button>

        <!-- Links adicionais -->
        <div class="mt-4 flex justify-between text-sm">
            <a href="/login/esqueci-senha" class="text-blue-500 hover:underline">Esqueci a senha</a>
            <a href="/login/cadastro" class="text-blue-500 hover:underline">Cadastrar</a>
        </div>
    </form>
</main>
